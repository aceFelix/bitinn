<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itniuma.bitinn.mapper.ArticleMapper">
    <select id="list" resultType="com.itniuma.bitinn.pojo.Article">
        select * from article
        <where>
            <if test="userId != null">
                create_user = #{userId}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="state != null and state != ''">
                and state = #{state}
            </if>
        </where>
        order by update_time desc
    </select>
</mapper>
